# Default values for helm-rails.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  repository: my-rails-dev
  tag: latest
  pullPolicy: IfNotPresent
service:
  type: NodePort
  internalPort: 3000
